// membuat array names
const names = ["Michael", "Hannah", "Jonas"];

/**
 * Menjalankan method map
 * method map untuk transform/mengubah data
 */


const formattedName = names.map((name) => console.log(`Mr/Mrs. ${name}`));