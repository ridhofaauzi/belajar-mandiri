// membuat variable array menggunakan const 
const animals = ["cat", "bird", "fish", "bird"];

// looping array menggunakan for of
for (const animal of animals) {
    console.log(`Hewan : ${animal}`);
}